# Open ProjectPig in pycharm, python setup
```
!git clone -b {BRANCH_NAME} https://{TOKEN}@github.com/Rapirys/ProjectPIG.git
%cd ProjectPIG
%pip install swig
%pip install -r requirements.txt

%cd mienerl
%pip install -e --no-deps .
```

# Run minerl/scripts/setup_intelij
# Open minerl/minerl in intelij